  set_sg1_min_soc:
    name: SG1 set min SoC
    min: 0
    max: 50
    step: 1

  set_sg1_max_soc:
    name: SG1 set max SoC
    min: 50
    max: 100
    step: 1

  set_sg1_reserved_soc_for_backup:
    name: SG1 set reserved SoC for backup
    min: 0
    max: 75
    step: 1

  set_sg1_battery_forced_charge_discharge_power:
    name: SG1 set forced charge discharge power in W
    min: 0
    max: !secret sungrow_sg1_max_forced_power
    step: 100

  set_sg1_battery_max_charge_power:
    name: SG1 set max battery charge power in W
    min: 100
    max: !secret sungrow_sg1_max_charge_power
    step: 100

  set_sg1_battery_max_discharge_power:
    name: SG1 set max battery discharge power in W
    min: 100
    max: !secret sungrow_sg1_max_discharge_power
    step: 100

  set_sg1_battery_charging_start_power:
    name: SG1 set battery charging start power in W
    min: 0
    max: 1000
    step: 10

  set_sg1_battery_discharging_start_power:
    name: SG1 set battery discharging start power in W
    min: 0
    max: 1000
    step: 10
